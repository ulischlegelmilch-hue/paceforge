{
  "name": "@paceforge/server",
  "version": "0.0.1",
  "private": true,
  "description": "PaceForge Phase-2-Backend (Fastify): LLM-gestützte NL-Zieleingabe mit Regel-Fallback; Basis für Garmin-OAuth + Sync",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "@paceforge/core": "*",
    "fastify": "^5.2.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
